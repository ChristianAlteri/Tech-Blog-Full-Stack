function setTimeout() {
    // Prompt the user to recommend something
    const recommendation = prompt("Recommend us to a family member:");
  
    // Get the email address from the user
    const email = prompt("Enter your email address:");