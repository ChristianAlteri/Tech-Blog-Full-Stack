{{#if logged_in }}
<section>
  <h1>📜 Dashboard📜 </h1>
  <h3>Here you can upload to the blog 📝</h3>
  <h3> </h3>
  <h3> </h3>
{{>post_form}}
{{#each (reverse posts) as |post|}}
      {{> post_card 
        title=post.title
        id=post.id
        body=post.body
        date_created=post.date_created
        user=user.name
      }}
      {{> comment_form
      post_id=post.id
      }}
      {{#each post.comments as |comment|}}

        {{> comment_card 
        body=comment.body 
        user=post.user.name
        }}
      {{/each}}
      <button style="background-color: #b790de; border-radius: 50px; max-width: 200px;" href="/post/{{this.post.id}}/update" type='submit'>Update Post</button>
      <form method="POST" action="api/post/{{post.id}}">
      <button style="background-color: #b790de; border-radius: 50px; max-width: 200px;" type="submit">Delete Post</button>
</form>
{{/each}}
</section>
 {{else}}

  <p><a href="/home/login">Login </a> to upload to the blog or<a href="/home/signup"> Sign up</a></p>
  

{{/if}}